# 每步数据清单

> 目标：这份文档不是讲“业务日志”，而是讲“每个 step 里到底有多少工作量可并行化”。  
> 后续如果要把当前流仿真进一步改成更符合 ECS / GPU 多实体并行的结构，这些数据可以用来判断：
> 1. 每一步真实被触达的数据规模有多大。
> 2. 工作是集中在少数端口，还是分散在很多端口。
> 3. 当前挂在 `Sim` 上的全局数组 / 队列，是否值得拆成更多瞬时实体或更多并行 archetype。

---

## 1. 先说结论：我们想统计什么

不是所有“总量”都值得打印。

例如：
- `numFlowDefs`
- `numPorts`
- `numTopoNodes`

这些更像“静态规模”，启动时看一次就够了；它们不能说明**某一步**有没有足够并行度。

真正值得按 step 打印的是两类量：

1. **本步真实进入流水线的工作量**
- 例如本步有多少 ready flow、多少 due event、多少 dirty port、多少 active tag。

2. **本步可能被拆成更多实体 / 更多并行 worker 的中间队列规模**
- 例如 create request、cleanup request、completion request、outbox event 的数量。

---

## 2. 当前 step 流水线里哪些地方最值得观察

当前任务图在 [src/sim_tasks.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_tasks.cpp) 里，主顺序可以简化成：

1. `schedulePendingFlows`
2. `deliverEvents`
3. ingress chain
- `pfcPropagateOnePort`
- `flowArrivalOnePort`
- `flushTagCreate`
- `bwUpdateOnePort`
- `flushTagCreate`
- `flushTagCleanup`
- `flushFlowCompletion`
- `flushPortOutbox`
4. alloc chain
5. pfc / emit chain
6. clear / snapshot / chooseDT
7. buffer advance
8. `flowProgressAndCleanupSystem`

其中，后续如果要继续 ECS 化，最有价值的是下面这些“工作单元”：

- flow 启动
- due event 分发
- per-port ingress 处理
- per-port alloc / emit / pfcDetect
- source tag 完成
- deferred request 队列的 flush

---

## 3. 推荐的每步数据字段

建议先做一个 `V1` 版，一行打印一次 step，全都打“总量 + 有效分布规模”。

推荐字段分 5 组：

1. 调度规模
2. 事件规模
3. 端口并行规模
4. 延迟队列 / 中间请求规模
5. 活跃状态规模

下面逐个解释。

---

## 4. 字段说明

## 4.1 调度规模

来源：
- [src/sim_systems_schedule.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_schedule.cpp)

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `ready_flows` | 本步 `pendingFlows` 中满足 `start_time <= now` 的流数量。 | 代表“本步有多少 flow start 工作”进入系统。后续如果想把 flow 注入拆成更多并行单元，这个数是最直接的工作量指标。 |
| `scheduled_events` | 本步由 `ready_flows` 成功转成的首个 `Arrival` 事件数量。 | 用来区分“有多少流到了开始时间”和“最终真被成功注入了多少个事件”。 |
| `pending_flows_after` | 本步调度完成后还剩多少 pending flow。 | 用来观察系统是否长期积压在 flow 启动阶段。不是并行度指标本身，但能辅助判断 step 是否被启动流塞满。 |

为什么值得看：
- 如果 `ready_flows` 长期都很小，比如每步只有 `0~2` 个，那么“把 flow 启动拆成实体”通常收益有限。
- 如果某些 step `ready_flows` 成批出现，例如几十、几百一起启动，那就值得考虑更细粒度的 flow-start worker。

---

## 4.2 事件规模

来源：
- [src/sim_systems_dispatch.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_dispatch.cpp)

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `due_events` | 本步 `delayedEvents` 中满足 `t <= now` 的事件总数。 | 代表本步真正进入事件分发链的总工作量。 |
| `due_arrival` | 本步 due event 里 `Arrival` 的数量。 | 说明这一步新增 tag / 流到达工作多不多。 |
| `due_bwupdate` | 本步 due event 里 `BwUpdate` 的数量。 | 说明这一步带宽传播、清理、完成判断工作多不多。 |
| `due_pfc` | 本步 due event 里 `PfcControl` 的数量。 | 说明这一步 PFC 控制链是否活跃。 |
| `due_ports` | 本步收到至少 1 个事件的 port 数量。 | 这是最关键的分布指标。事件很多但都打到一个 port，上 GPU 的并行度依然差。 |
| `delayed_events_after` | 分发完后剩余未到时事件数量。 | 用来观察 delayed queue 是否长期堆积。 |

为什么值得看：
- `due_events` 告诉你“总工作量”。
- `due_ports` 告诉你“工作分散程度”。
- 真正决定 per-port fanout 值不值的是 `due_ports`，不是单纯 `due_events`。

---

## 4.3 端口并行规模

来源：
- [src/sim_systems_dispatch.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_dispatch.cpp)
- [src/sim_systems_buffer.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_buffer.cpp)
- [src/sim_systems_bandwidth.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_bandwidth.cpp)
- [src/sim_tasks.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_tasks.cpp)

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `dirty_ports` | 本步真正处于 dirty 状态、需要参与后续端口处理的 port 数量。 | 这是 per-port 并行最核心的规模指标。 |
| `last_dirty_ports` | `snapshotDirtyPorts` 后记录下来的上一步脏端口数。 | 用来观察 dirty port 集合在步间的传播规模。 |
| `alloc_ports` | 本步 alloc 阶段实际处理到的 port 数量。 | 有些 port 虽然存在，但没有 tag 或没有脏状态，不构成真实并行工作。 |
| `alloc_tags` | alloc 阶段参与带宽分配的 tag 总数。 | 这是“port 内部工作量”的核心指标。 |
| `emit_ports` | 本步 emit 阶段真正产生下游事件的 port 数量。 | 用来判断 emit fanout 是否值得继续拆粒度。 |
| `buffer_ports` | 本步 buffer advance 阶段真正推进了 buffer 的 port 数量。 | 用来判断 buffer 阶段是否有足够多的并行端口。 |

为什么值得看：
- `dirty_ports` 低，说明 step 工作集中，per-port GPU 并行度可能不够。
- `alloc_tags` 高但 `alloc_ports` 低，说明热点集中在少数端口，后续可能要考虑“port 内 tag 并行”而不只是“port 间并行”。

---

## 4.4 延迟队列 / 中间请求规模

来源：
- [src/sim_systems_ingress.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_ingress.cpp)
- [src/sim_systems_dispatch.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_dispatch.cpp)

这些字段特别重要，因为它们正是当前还挂在 `Sim` 上、以后最可能被继续 ECS 化的数据。

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `create_reqs` | 本步所有 `PortCreateList` 里的待建 tag 请求总数。 | 代表“当前串行 flushTagCreate 在消化多大规模的 deferred create 队列”。 |
| `create_ports` | 本步至少有 1 个 create request 的 port 数量。 | 说明创建请求是集中在少数 port，还是广泛分布。 |
| `cleanup_reqs` | 本步所有 `PortCleanup` 里的待销毁 tag 请求总数。 | 代表 deferred destroy 工作量。 |
| `cleanup_ports` | 本步至少有 1 个 cleanup request 的 port 数量。 | 判断 destroy/cleanup 是不是适合进一步拆成并行工作。 |
| `completion_reqs` | 本步所有 `PortCompletionList` 里的流完成请求总数。 | 代表“本步 FCT 写入工作量”。 |
| `completion_ports` | 本步至少有 1 个 completion request 的 port 数量。 | 判断完成记录是集中还是分散。 |
| `outbox_events` | 本步所有 `PortOutbox` 中待 flush 的事件总数。 | 表示 emit / pfcDetect 之后，准备回灌到 `delayedEvents` 的事件规模。 |
| `outbox_ports` | 本步至少写了 1 个 outbox event 的 port 数量。 | 判断 outbox 工作是否真的分散在大量端口上。 |

为什么值得看：
- 如果 `create_reqs / cleanup_reqs / outbox_events` 经常很小，那么继续把它们拆成“独立 transient entity”收益不一定高。
- 如果这些数经常成百上千，并且 `*_ports` 也大，那后续实体化 / 并行 flush 才更有意义。

---

## 4.5 活跃状态规模

来源：
- [src/sim.hpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim.hpp)
- [src/sim_systems_progress.cpp](/home/zhangran/work/madrona5/madrona_simple_example/src/sim_systems_progress.cpp)

| 字段 | 含义 | 作用 |
| --- | --- | --- |
| `active_tags` | 当前全局活跃 tag 总数，等价于 `numTagIndexEntries`。 | 代表系统当前的动态规模上限。 |
| `source_tags` | 当前 source tag 数量。 | 直接影响 `progressFinishedSources` 的扫描规模。 |
| `ingress_tags` | 当前 ingress tag 数量。 | 反映 ingress 相关扫描和 PFC 相关扫描的规模。 |
| `finished_sources` | 本步真正完成的 source tag 数量。 | 说明“流完成工作”每步有多大。 |
| `emitted_cleanup` | 本步由于 source 完成而发出的 cleanup `BwUpdate(0)` 事件数。 | 反映完成后清理链的工作量。 |
| `drain_timers` | 当前活跃 backlog drain timer 数量。 | 说明 timer 扫描里有多少真实活跃项。 |
| `pause_timers` | 当前活跃 PFC pause timer 数量。 | 衡量 PFC pause 链工作量。 |
| `resume_timers` | 当前活跃 PFC resume timer 数量。 | 衡量 PFC resume 链工作量。 |
| `next_dt` | 本步选出的下一小步时间。 | 不是并行度指标，但能帮助解释“为什么 step 数很多 / 为什么工作被切得很碎”。 |

为什么值得看：
- `active_tags` 大，但 `dirty_ports` 小，通常意味着“状态很多，但真正每步被触达的不多”。
- `source_tags` 长期很大，说明 `progressFinishedSources` 这种串行扫描值得重点优化。

---

## 5. 最推荐的 V1 打印格式

建议第一版不要把日志分散打印到每个 phase。
建议做成一条统一的 step 工作量摘要，例如：

```text
[STEP_WORK] step=123 now=1.234 ready_flows=16 scheduled_events=16 due_events=24 due_arrival=12 due_bwupdate=10 due_pfc=2 due_ports=18 active_tags=540 source_tags=64 ingress_tags=476 dirty_ports=22 alloc_ports=15 alloc_tags=180 create_reqs=9 create_ports=4 cleanup_reqs=7 cleanup_ports=3 completion_reqs=3 completion_ports=2 outbox_events=18 outbox_ports=11 finished_sources=4 emitted_cleanup=4 drain_timers=6 pause_timers=0 resume_timers=0 next_dt=0.0005
```

这样做有几个好处：

1. 好读
- 一眼就能看出某步是事件重、端口重，还是 tag 重。

2. 好对比
- 后续跑不同拓扑、不同流规模、不同 QoS/PFC 配置时，可以直接横向比较。

3. 对性能扰动小
- 比逐 phase 打很多细碎日志更轻，也更适合后面改成“先写统计结构，最后统一导出/打印”。

---

## 6. 这些字段如何帮助判断“值不值得继续 ECS 化”

可以按下面思路解读：

### 情况 A：`due_events` 小、`due_ports` 也小

说明：
- 每步进入系统的事件本来就不多。
- 而且工作集中在少数 port。

结论：
- 单纯继续把更多中间数组拆成实体，收益可能有限。
- 更该关注减少串行扫描、减少无效遍历、降低每步固定成本。

### 情况 B：`due_events` 大，但 `due_ports` 小

说明：
- 工作量有，但热点强烈集中在少数 port。

结论：
- “port 间并行”未必是瓶颈。
- 更值得研究的是“port 内 tag 粒度并行”或更细的局部结构。

### 情况 C：`dirty_ports` 大、`alloc_ports` 大、`outbox_ports` 也大

说明：
- 每步确实有大量端口同时活跃。

结论：
- 当前模型很适合继续沿 per-port ECS / GPU fanout 优化。
- 这时继续消除串行 flush / reduction 的收益通常比较明确。

### 情况 D：`create_reqs / cleanup_reqs / outbox_events` 长期很大

说明：
- 当前 `Sim` 上的 deferred queue 已经成了真实的工作池。

结论：
- 这些结构是后续继续 ECS 化的优先候选。
- 可以考虑把它们从“全局数组 + 串行 flush”进一步改成更并行的工作形式。

### 情况 E：`active_tags` 很大，但 `finished_sources / due_events / dirty_ports` 很小

说明：
- 系统里状态很多，但每步真正活跃的工作集不大。

结论：
- 要特别小心“为了 ECS 而 ECS”。
- 这类场景下，很多优化更像“减少扫描范围”“缩小 working set”，而不是继续增加实体数。

---

## 7. 不建议第一版就打印的字段

下面这些不是没价值，而是更适合第二阶段：

- 每个 port 的详细计数
- 每个 priority 的 buffer / pause 细分计数
- 每条 flow 的逐步生命周期统计
- 每个事件类型的按节点 / 按端口直方图

原因：
- 第一版目标是判断“并行工作量够不够”，不是做完整性能画像。
- 一上来打印太细，会让日志噪音远大于结论价值。

---

## 8. 实现建议

建议不要把这些统计直接写成大量 `printf`。

更稳的做法是：

1. 定义一个每步统计结构
- 例如 `StepWorkloadStats`

2. 在各个 phase 内只做计数累计
- 不直接打印

3. 在 step 尾部统一输出一行
- 或导出到 host 后再打印

这样做的好处：
- 对运行性能扰动更小
- 日志格式更稳定
- 后续更容易改成 CSV / Python 读取 / 可视化

---

## 9. 推荐的第一版最小字段集合

如果只想先做最小闭环，建议先上这些：

- `ready_flows`
- `scheduled_events`
- `due_events`
- `due_ports`
- `dirty_ports`
- `alloc_ports`
- `alloc_tags`
- `create_reqs`
- `cleanup_reqs`
- `completion_reqs`
- `outbox_events`
- `active_tags`
- `source_tags`
- `finished_sources`
- `drain_timers`
- `pause_timers`
- `resume_timers`
- `next_dt`

这是最小但仍然足够判断“当前流仿真是否适合进一步面向 GPU 多实体优化”的一组数据。
