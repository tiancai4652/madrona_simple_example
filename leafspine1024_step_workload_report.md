# leafspine1024 `step_workload.csv` 分析报告

## 1. 实验对象与数据来源

- 工作负载文件：`/home/zhangran/work/madrona5/exp/out/madrona_leafspine1024_stepwork-cpu/step_workload.csv`
- 拓扑文件：`/home/zhangran/work/madrona5/jiuding_dodsim/examples/leafspine1024_lowdelay/leafspine_h1024_topo_oversub.txt`
- 流文件：`/home/zhangran/work/madrona5/jiuding_dodsim/examples/leafspine1024_lowdelay/leafspine_h1024_d64_alltoall_2m.txt`

本报告的目标，是判断当前这组流仿真在每个 step 中到底有多少“可并行处理的工作量”，以及这些工作量是否足以支撑后续向 ECS / GPU 多实体结构优化。

## 2. 基本规模

- 总 step 数：`22`
- 节点数：`1072`
- Host 数：`1024`
- Switch 数：`48`
- 输入链路数：`1536`
- 估算有向端口数：`3072`
- Flow 总数：`64512`

流的启动时间分布非常集中，只分成两波：

- `0.002` 秒启动：`31744` 条 flow
- `0.004` 秒启动：`32768` 条 flow

这意味着该 workload 不是“持续均匀注入”，而是非常明显的“成批注入 + 成批传播 + 成批收尾”结构。

## 3. 关键字段统计

下表只列对并行度判断最重要的字段。

| 字段 | 含义 | 活跃 step 平均值 | 最大值 | 说明 |
| --- | --- | ---: | ---: | --- |
| `ready_flows` | 本 step 新进入系统、可开始处理的 flow 数 | `32256` | `32768` | 只在 2 个 step 非 0，说明注入是大批量突发的 |
| `due_events` | 本 step 到期、需要处理的事件数 | `37888` | `64512` | 事件层面的主并行工作量来源 |
| `due_ports` | 本 step 有事件到达、需要激活的端口数 | `938.7` | `1536` | 端口级并行度很可观 |
| `dirty_ports` | 本 step 被触发、需要更新的端口数 | `768.05` | `1536` | 可视为端口处理前沿规模 |
| `alloc_ports` | 本 step 需要执行分配/仲裁的端口数 | `768.06` | `1536` | 后续很适合做端口实体化 |
| `alloc_tags` | 本 step 参与分配的 tag 数 | `36548` | `65536` | 说明单步内端口内部工作量也不小 |
| `create_reqs` | 本 step 新建请求数 | `32426.7` | `32768` | 当前若串行 flush，会比较重 |
| `cleanup_reqs` | 本 step 清理请求数 | `32512` | `32768` | 收尾阶段同样有明显批量性 |
| `outbox_events` | 本 step 输出事件数 | `37156.6` | `64512` | 事件传播本身也是大批量 |
| `outbox_ports` | 本 step 有输出的端口数 | `731.4` | `1024` | 输出侧也有稳定的端口并行度 |
| `active_tags` | 系统中仍保留的活跃 tag 数 | `104192` | `194560` | 说明系统状态体量并不小 |
| `source_tags` | 当前源端相关 tag 数 | `46648.9` | `64512` | 可用于判断源端路径的并行规模 |
| `ingress_tags` | 当前 ingress 相关 tag 数 | `87578.9` | `130048` | ingress 侧状态规模尤其大 |
| `finished_sources` | 本 step 完成的源 flow 数 | `32256` | `32768` | 只在 2 个 step 非 0，结束也呈突发批量 |

## 4. 几个最重要的比例

这些比例比绝对值更能说明“单个实体内部有没有足够工作量”。

### 4.1 `alloc_tags / alloc_ports`

- 最小值：`31`
- 中位数：`64`
- 平均值：`53.04`
- 最大值：`64`

含义：一个活跃分配端口，平均往往要处理几十个 tag。也就是说，并行机会不只存在于“端口之间”，也存在于“端口内部 tag 集合之间”。

### 4.2 `due_events / due_ports`

- 最小值：`31`
- 中位数：`42`
- 平均值：`44.17`
- 最大值：`64`

含义：一个活跃端口在一个 step 内通常不是只处理 1 个事件，而是成批处理几十个事件。这对 GPU 或多实体调度是利好。

### 4.3 `outbox_events / outbox_ports`

- 最小值：`31`
- 中位数：`64`
- 平均值：`54.57`
- 最大值：`64`

含义：输出事件也明显呈现“每个活跃端口带一批事件”的模式，因此 outbox 路径如果还保留串行整理，会是值得优化的热点。

## 5. Step 阶段特征

### 5.1 第一波注入：Step 2

- `ready_flows = 31744`
- `due_events = 31744`
- `due_ports = 1024`
- `dirty_ports = 1024`
- `alloc_ports = 1024`
- `alloc_tags = 31744`
- `create_reqs = 31744`
- `outbox_events = 31744`

这一步已经很说明问题：仅一次注入，就同时激活了 `1024` 个端口，并产生了 `31744` 级别的 tag / event 工作量。即使只做端口级并行，也不是小规模。

### 5.2 第二波注入：Step 4

- `ready_flows = 32768`
- `due_events = 32768`
- `alloc_tags = 64512`
- `outbox_events = 64512`

第二波注入进一步抬高了系统内同时活跃的工作量。这里 `alloc_tags` 和 `outbox_events` 都冲到了 `64512`，说明系统在这一阶段非常适合做批处理式并行执行。

### 5.3 峰值传播阶段：Step 5 和 Step 10

两步都出现了最强的事件和端口活跃度：

- `due_events = 64512`
- `due_ports = 1536`
- `dirty_ports = 1536`
- `alloc_ports = 1536`

这说明最繁忙的时候，有 `1536` 个端口同时需要参与计算。如果后续把端口处理做成实体，这已经是比较像 GPU/多线程批处理的形状了。

### 5.4 状态峰值：Step 7

- `active_tags = 194560`
- `alloc_tags = 64512`

这一步说明系统内“保留态”非常大，不只是瞬时事件多。后续优化如果还保留大范围全表扫描，成本会高；应尽量转成只遍历 touched / active frontier 的结构。

### 5.5 完成波峰：Step 8 和 Step 11

- Step 8: `finished_sources = 31744`
- Step 11: `finished_sources = 32768`

结束阶段同样是成批完成，不是零散回收。这意味着 cleanup、completion 相关路径也可能值得实体化或并行 flush。

### 5.6 尾部稀疏阶段：Step 12 到 Step 20

这个阶段最值得注意的特点是：

- `active_tags` 仍然很大，约在 `65k` 到 `98k` 量级
- 但 `dirty_ports` 已经急剧下降，低时只有 `1`、`511`、`512`
- `next_dt` 出现非常小的微步进，最小约 `4.7e-05`

这说明后半段呈现出明显的“状态很多，但真正活跃前沿很小”的特征。

这类阶段对架构设计的含义很明确：

- 不能依赖全量扫描所有 tag / port
- 必须保留稀疏 touched-set / frontier 结构
- 否则前期并行收益，可能会被尾部阶段的大量空扫抵消

## 6. 对 ECS / GPU 化可行性的判断

结论是：**这组 workload 明显值得继续朝多实体 / GPU 方向优化，但优化重点应该是“高峰期批量并行 + 尾部稀疏前沿保留”，而不是单纯把所有逻辑机械拆成实体。**

原因如下。

### 6.1 支持继续优化的证据

- 注入阶段一次就是 `3.1w` 到 `3.2w` 条 flow，就绪工作量足够大
- 峰值阶段一次可处理 `6.4w` 级事件
- 峰值阶段同时激活 `1536` 个端口
- 单个活跃端口平均还挂着几十个事件 / tag
- `create_reqs`、`cleanup_reqs`、`outbox_events` 都是 `3w` 到 `6w` 级别

这不是一个“并行实体数量明显不够”的 workload。至少在前中段，它有很强的批量并行特征。

### 6.2 需要谨慎的地方

后半段 active state 仍大，但 active frontier 已经很小。这意味着：

- 若把数据结构改成 ECS 后仍需要频繁扫全量组件，收益可能不理想
- 真正关键的是让系统只处理“这一步真的被触发的端口 / tag / ingress”
- 尾部微步进较多，调度和同步开销也需要控制

所以问题不是“适不适合多实体”，而是“应该用什么粒度、什么激活方式去做多实体”。

## 7. 对当前指标的几个解释

### 7.1 `completion_reqs = 0` 不等于没有完成

这组数据里虽然 `completion_reqs` 没有体现出工作量，但 `finished_sources` 在 Step 8 和 Step 11 明确出现了两次大批量完成。因此：

- `completion_reqs` 只代表特定路径上的请求统计
- 不能把它当成“完成事件是否存在”的总代表

### 7.2 PFC 相关结论暂时得不出来

本次数据中：

- `due_pfc = 0`
- `pause_timers = 0`
- `resume_timers = 0`

所以这份 workload 只能用于分析普通事件 / 端口 / tag 传播负载，暂时不能用于评估 PFC 相关并行行为。

## 8. 后续优化建议

建议后续 ECS / GPU 化优先看下面四类热点。

### 8.1 优先去掉全局扫描

重点关注：

- `active_tags`
- `source_tags`
- `ingress_tags`

这些集合规模很大，但后期真正活跃部分很小。如果当前代码里存在按 step 全量扫描，这会直接限制优化收益。

### 8.2 把端口处理作为第一层实体并行

因为 `due_ports`、`dirty_ports`、`alloc_ports` 在峰值能到 `1536`，端口天然就是第一层比较稳妥的实体切分单位。

### 8.3 对热点端口进一步做 tag 级拆分

单端口常常同时挂 `31` 到 `64` 个 tag / event。若端口内部逻辑较重，可以考虑：

- 端口为一级实体
- tag 或请求记录为二级并行对象

是否值得继续往下拆，要看端口内部更新是否彼此独立，以及同步代价是否可控。

### 8.4 重构批量 flush 路径

重点关注：

- `create_reqs`
- `cleanup_reqs`
- `outbox_events`

这些路径的峰值都已经到 `3w` 到 `6w` 量级。如果当前还是串行收集、串行写回、串行搬运，会比较容易成为瓶颈。

## 9. 总结

从这份 `step_workload.csv` 看，当前流仿真并不是“事件太少，不适合多实体”的类型。相反，它在前中段具有非常明显的批量并行特征：

- 批量 flow 注入
- 批量事件到期
- 大量端口同时活跃
- 单端口内部还有几十个 tag / event

因此，**后续朝 ECS / GPU 结构优化是有价值的**。

但同时，这份数据也明确提醒了一点：**后期 step 会进入“状态很大、前沿很小”的阶段**。所以优化的核心不是简单增加实体数量，而是：

- 让实体只在被触发时参与调度
- 保留稀疏前沿
- 避免对大状态做无差别全量扫描

如果后续要进一步做更细的设计判断，建议下一步再补两类统计：

- 每步“真正进入端口计算函数”的端口数量与每端口处理时长
- 每步 create / cleanup / outbox 三类队列从生成到 flush 的分布

这样可以更准确地区分：

- 哪些地方适合做实体并行
- 哪些地方本质上是串行汇聚瓶颈
